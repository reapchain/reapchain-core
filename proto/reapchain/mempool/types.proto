syntax = "proto3";
package reapchain.mempool;

option go_package = "gitlab.reappay.net/sucs-lab/reapchain/proto/reapchain/mempool";

message Txs {
  repeated bytes txs = 1;
}

message Message {
  oneof sum {
    Txs txs = 1;
  }
}
