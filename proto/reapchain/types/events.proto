syntax = "proto3";
package reapchain.types;

option go_package = "github.com/tendermint/tendermint/proto/reapchain/types";

message EventDataRoundState {
  int64  height = 1;
  int32  round  = 2;
  string step   = 3;
}
